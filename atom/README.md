# Atom plugins playground

## Install

```sh
$ ln -s $PWD $HOME/.atom/packages/atom-plugins-playground
# ctrl+alt+r reload atom
```

## Keymap

```sh
$ cat ~/.atom/keymap.cson

'atom-text-editor':
  'ctrl-:': 'insert-previous-directory:toggle'
```
